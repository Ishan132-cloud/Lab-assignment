Part9:
Deleting a symbolic link does not affect the original file because a symbolic link is a separate file that
 simply points to the path of the original.

Command rm sample_soft.txt (or unlink sample_soft.txt) used to delete the symbolic link sample_soft.txt

Verification: Run ls sample_data.txt to confirm the original still exists.